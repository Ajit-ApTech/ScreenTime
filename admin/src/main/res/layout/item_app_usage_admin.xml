<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    app:cardBackgroundColor="@color/bg_card"
    app:cardCornerRadius="14dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/card_stroke"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="12dp">

        <!-- App Initial Avatar Circle -->
        <FrameLayout
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:background="@drawable/bg_avatar_circle">

            <TextView
                android:id="@+id/tvAppInitial"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="A"
                android:textColor="@color/accent_blue_light"
                android:textSize="16sp"
                android:textStyle="bold" />
        </FrameLayout>

        <!-- Details -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvAppName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="App Name"
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvPackageName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:text="com.example.app"
                android:textColor="@color/text_secondary"
                android:textSize="12sp" />

            <!-- Duration Pill -->
            <TextView
                android:id="@+id/tvUsageTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="5dp"
                android:background="@drawable/bg_pill_neutral"
                android:paddingHorizontal="8dp"
                android:paddingVertical="3dp"
                android:text="2h 32m"
                android:textColor="@color/accent_blue_light"
                android:textSize="11sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Circular Action Buttons -->
        <ImageButton
            android:id="@+id/btnEditUsage"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:background="@drawable/bg_btn_circle"
            android:contentDescription="Edit usage time"
            android:padding="8dp"
            android:src="@android:drawable/ic_menu_edit"
            app:tint="@color/accent_blue_light" />

        <ImageButton
            android:id="@+id/btnDeleteUsage"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_marginStart="6dp"
            android:background="@drawable/bg_btn_circle_red"
            android:contentDescription="Delete app session"
            android:padding="8dp"
            android:src="@android:drawable/ic_menu_delete"
            app:tint="@color/status_red" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
